document.addEventListener("DOMContentLoaded", () =>{
    console.log("DOM!")
}

)

const productos= productos.map(productos=> new productos(producto))
const productos = new productos (newproductos)})

